import React from "react";

const NoResults = props => (
  <li className="not-found">
    <h3>No Results Found</h3>
    <p>Your search did not return any results. Sorry about that.</p>
  </li>
);

export default NoResults;
